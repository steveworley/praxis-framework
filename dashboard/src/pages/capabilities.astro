---
/**
 * Legacy /capabilities route — redirects to /role#capabilities. The tool
 * catalog (chat tools, MCP servers, verbs, reference data) is now a deep
 * section on the role surface, sitting beneath the PD "Capabilities" pill.
 */
export const prerender = false;
return Astro.redirect('/role#capabilities');
---
