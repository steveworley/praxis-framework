---
/**
 * Root route — redirects to the canonical home surface, /chat. Per the nav
 * consolidation pass (10 tabs → 7), /chat is now the runtime entry point and
 * the dashboard's home affordance is the PRAXIS logo in the brand strip.
 *
 * The summary cards that previously lived here have been absorbed into
 * per-surface stats per `docs/future/dashboard-redesign.md` Phase 2.
 */
export const prerender = false;
return Astro.redirect('/chat');
---
